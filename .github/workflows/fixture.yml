name: Ground Truth P23-E03
on:
  workflow_dispatch:
permissions:
  contents: write
jobs:
  fixture:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - name: Ground-truth target operation
        env:
          GH_TOKEN: ${{ github.token }}
        run: git push origin HEAD:main
